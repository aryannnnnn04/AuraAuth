"""
Utility functions for the Streamlit frontend application.

This module provides helper functions for:
- Data formatting and display
- Session state management
- Plot/visualization utilities
- Common calculations and transformations
- UI component helpers

Currently a placeholder for future utility functions.
As the frontend grows, helper functions should be added here to avoid
cluttering the main app.py file.

Example utilities that could be added:
- format_metric(value, metric_type) -> str
- format_dataframe(df) -> pd.DataFrame (formatted for display)
- get_color_by_score(score) -> str (color coding)
- safe_division(numerator, denominator) -> float (with zero handling)
- truncate_text(text, max_length) -> str
"""

# TODO: Add utility functions here as the frontend grows
# Consider organizing by category:
# - formatting_utils: format_metric, format_dataframe, truncate_text
# - calculation_utils: safe_division, percentile_rank, confidence_interval
# - color_utils: get_color_by_score, get_status_color
# - session_utils: initialize_session_state, update_session_value

